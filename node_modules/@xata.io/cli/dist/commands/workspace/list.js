var _a;
import { BaseCommand } from '../../base.js';
class WorkspaceList extends BaseCommand {
    async run() {
        const xata = await this.getXataClient();
        const workspacesList = await xata.api.workspaces.getWorkspacesList();
        if (this.jsonEnabled())
            return workspacesList.workspaces;
        const headers = ['Name', 'Id', 'Role'];
        const rows = workspacesList.workspaces.map((b) => [b.name, b.id, b.role]);
        this.printTable(headers, rows);
    }
}
_a = WorkspaceList;
WorkspaceList.description = 'List your workspaces';
WorkspaceList.examples = [];
WorkspaceList.flags = {
    ..._a.commonFlags
};
WorkspaceList.args = {};
WorkspaceList.enableJsonFlag = true;
export default WorkspaceList;
